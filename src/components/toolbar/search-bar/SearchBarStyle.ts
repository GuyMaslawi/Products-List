import { makeStyles } from '@material-ui/core';

export const FieldStyle = makeStyles({
    root: {
        '& label': {
            fontSize: '1.5rem'
        },
        '& .MuiInputBase-root': {
            fontSize: '1.5rem'
        }
    }
});